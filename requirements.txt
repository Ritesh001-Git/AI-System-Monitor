# Core Libraries
Flask==2.3.2               # Web framework for backend
Flask-SocketIO==5.3.6      # Real-time WebSocket communication
psutil==5.9.5              # System monitoring (CPU, memory, disk, etc.)
scikit-learn==1.3.0        # Machine learning models (Linear Regression, etc.)
pandas==2.0.3              # Data manipulation and preprocessing
numpy==1.24.3              # Numerical computations for ML
joblib==1.3.2              # Model serialization and loading

# Frontend & Visualization
chart.js==3.9.1            # Interactive charts (via CDN, no pip install needed)
bootstrap==5.3.0           # Responsive UI (via CDN, no pip install needed)

# Testing & Debugging
pytest==7.4.0              # Unit testing framework
logging==0.4.9.6           # Logging system behavior

